# v1.0 quality-gate validation

This marker intentionally contains no runtime behavior. It exists so the full repository snapshot containing the G1-G5 Q1 quality gates is exercised by the repository's pull-request CI before final merge.
