[project]
name = "doctoral-research-os"
version = "1.0.0"
description = "API-first, human-gated Doctoral Research OS for topic selection, experiments, writing, review, venues and manual submission"
requires-python = ">=3.10"

[tool.unittest]
start-directory = "tests"
